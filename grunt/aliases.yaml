default:
    - 'concurrent:first'
    - 'concurrent:second'
    - 'concurrent:third'

pull:
    - 'gitpull:task'

install:
    - 'npm-install:install'

exp:
    - 'express'

test:
    - 'mochaTest:test'

ks-neo4j-start:
    - 'shell:start_neo4j'

ks-start:
    - 'shell:start_neo4j'
    - 'express'