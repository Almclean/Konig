default:
    - 'concurrent:first'
    - 'concurrent:second'
    - 'concurrent:third'

pull:
    - 'gitpull:task'

install:
    - 'npm-install:install'

exp:
    - 'express'

test:
    - 'mochaTest:dummy'